name: backend-go / workflow-service

# CR-DB-002/CR-DB-003 acceptance criteria: run workflow-service's
# migrations + integration tests on BOTH dialects (Postgres, MySQL) on
# every PR that touches it — copies backend-go-usage-service.yml's
# (the pilot's) structure verbatim, only path/service name changed, per
# specs/backend-go/crs/v4/multi-database/tasks/ROLLOUT-TRACKING.md's
# batch-3/4/5 rollout.
on:
  pull_request:
    paths:
      - "backend-go/common/**"
      - "backend-go/services/workflow-service/**"
      - "backend-go/go.work"
      - ".github/workflows/backend-go-workflow-service.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        dialect: [postgres, mysql]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - name: Install golang-migrate CLI (${{ matrix.dialect }} driver only)
        run: |
          go install -tags '${{ matrix.dialect }}' github.com/golang-migrate/migrate/v4/cmd/migrate@latest
      - name: go build
        working-directory: backend-go/services/workflow-service
        run: go build ./...
      - name: go vet
        working-directory: backend-go/services/workflow-service
        run: go vet ./...
      - name: Unit tests (no Docker)
        working-directory: backend-go/services/workflow-service
        run: go test ./...
      - name: Integration tests (${{ matrix.dialect }}, testcontainers)
        working-directory: backend-go/services/workflow-service
        run: |
          if [ "${{ matrix.dialect }}" = "postgres" ]; then
            go test -tags=integration ./internal/adapter/postgres/... -v
          else
            go test -tags=integration ./internal/adapter/mysql/... -v
          fi
